<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grisClaro">

    <!-- Contenedor principal -->
    <LinearLayout
        android:id="@+id/layout_calendar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <!-- Card superior con navegación de semana -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:cardElevation="4dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="@color/blanco">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- Título -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Calendario Semanal"
                    android:textSize="14sp"
                    android:textColor="@color/grisMedio"
                    android:textStyle="bold"
                    android:letterSpacing="0.05"
                    android:layout_marginBottom="12dp" />

                <!-- Fila navegación -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <!-- Botón semana anterior -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_prev_week"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        app:icon="@drawable/ic_chevron_left"
                        app:iconTint="@color/verdeSantoTomas"
                        app:iconPadding="0dp"
                        app:iconGravity="textStart"
                        app:backgroundTint="@color/verdeClaro"
                        app:cornerRadius="24dp"
                        style="@style/Widget.Material3.Button.IconButton"
                        android:contentDescription="Semana anterior" />

                    <!-- Texto de semana actual -->
                    <TextView
                        android:id="@+id/tv_current_week"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:gravity="center"
                        android:text="14–20 DE OCTUBRE 2025"
                        android:textSize="17sp"
                        android:textStyle="bold"
                        android:letterSpacing="0.05"
                        android:textAllCaps="true"
                        android:textColor="@color/verdeSantoTomas"
                        android:fontFamily="sans-serif-medium"
                        tools:text="14–20 DE OCTUBRE 2025" />

                    <!-- Botón semana siguiente -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_next_week"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        app:icon="@drawable/ic_chevron_right"
                        app:iconTint="@color/verdeSantoTomas"
                        app:iconPadding="0dp"
                        app:iconGravity="textStart"
                        app:backgroundTint="@color/verdeClaro"
                        app:cornerRadius="24dp"
                        style="@style/Widget.Material3.Button.IconButton"
                        android:contentDescription="Semana siguiente" />

                </LinearLayout>

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- ViewPager2 con el calendario -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewpager_calendar"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:orientation="horizontal"
            android:background="@color/blanco" />

    </LinearLayout>

    <!-- Overlay de estado vacío -->
    <LinearLayout
        android:id="@+id/layout_empty_calendar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone"
        tools:visibility="visible">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_calendar"
            android:alpha="0.3"
            app:tint="@color/grisMedio" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/no_hay_actividades"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/grisMedio"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Agrega tu primera actividad usando el botón +"
            android:textSize="14sp"
            android:textColor="@color/grisMedio"
            android:gravity="center" />

    </LinearLayout>

    <!-- FloatingActionButton -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_new_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:src="@drawable/ic_add"
        app:tint="@color/blanco"
        app:backgroundTint="@color/verdeSantoTomas"
        app:elevation="6dp"
        app:borderWidth="0dp"
        android:contentDescription="@string/nueva_actividad" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
